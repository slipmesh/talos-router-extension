# Overlaid onto a siderolabs/extensions checkout (see Makefile's checkout-extensions
# target) at router/pkg.yaml. No kernel module involved - router is pure userspace (the
# `router` binary supervising a bundled `bird`), so unlike ../talos-awg-extension this
# package has no `dependencies: [image: ...]` on anything from ../talos-kernel at all -
# it's fully independent of that repo.
#
# BIRD is not built here. slipmesh/bird publishes it already built - statically linked
# against musl, from upstream source, with its own test suite run - so this pulls that
# image in as a plain OCI layer merge, the same way talos-awg-extension pulls the signed
# amneziawg module out of amneziawg-pkg. One BIRD build serves both, and neither the
# source download nor its checksum is this repository's problem any more.
#
# Both binaries are copied out. `router` never calls `birdc` - it speaks BIRD's
# control-socket protocol itself, over /run/bird.ctl - but `birdc` is what a human has to
# hand when debugging a node.
name: router
variant: scratch
shell: /bin/bash
dependencies:
  - stage: base
  - image: "{{ .BUILD_ARG_BIRD_IMAGE }}:{{ .BUILD_ARG_BIRD_IMAGE_TAG }}"
steps:
  - install:
      - |
        mkdir -p /rootfs/usr/local/lib/containers/router
        cp /bird /birdc /rootfs/usr/local/lib/containers/router/

        install -m 0755 /pkg/router-bin /rootfs/usr/local/lib/containers/router/router
        mkdir -p /rootfs/usr/local/etc/containers
        cp /pkg/router-service.yaml /rootfs/usr/local/etc/containers/router.yaml
    test:
      - |
        mkdir -p /extensions-validator-rootfs
        cp -r /rootfs/ /extensions-validator-rootfs/rootfs
        cp /pkg/manifest.yaml /extensions-validator-rootfs/manifest.yaml
        /extensions-validator validate --rootfs=/extensions-validator-rootfs --pkg-name="${PKG_NAME}"
finalize:
  - from: /rootfs
    to: /rootfs
  - from: /pkg/manifest.yaml
    to: /
